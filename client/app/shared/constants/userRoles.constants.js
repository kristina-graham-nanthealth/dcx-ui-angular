/** TODO: just random values for now. Needs to be developed but the framework is in place */
(function(){
    "use strict";

    angular.module("DCX")
           .constant("USER_ROLES", {
               ADMIN: "admin",
               EDITOR: "editor",
               GUEST: "guest"
           });

})();